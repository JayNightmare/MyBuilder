# Model path — absolute path or HuggingFace model ID
MODEL_PATH=./models/minehelper-v1

# Device for inference: "cuda" or "cpu"
DEVICE=cuda

# Maximum tokens the model can generate per request
MAX_TOKENS=4096

# Server binding
HOST=0.0.0.0
PORT=8000
